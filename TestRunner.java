import org.junit.runner.RunWith;
import org.junit.runners.Suite;

@RunWith(Suite.class)
@Suite.SuiteClasses({
    Dec2HexTest.class  // Add any other test classes you want to include in the suite
})
public class TestRunner {
    // No code needed here, annotations handle the suite
}
